1. Remote-GUI via browser
2. spacing and enter at the end of the query causes an error. Need to add `[query].strip()` 
3. for direct queries - would like to see stats 
4. support for blobs (I think this might just have to do with volumes?)
5. can we cleanup branches(?)
6. when using Client, it'd be great if the user can bookmark the query
